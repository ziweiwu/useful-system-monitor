# Dev-only measurement and comparison tools. Never published, never a dependency
# of the binary.
[package]
name = "sysmon-harness"
version = "0.0.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish = false

[dependencies]
sysmon-core = { path = "../sysmon-core" }
sysmon = { path = "../sysmon" }
serde_json = "1"
portable-pty = "0.8"
vt100 = "0.15"

[lints]
workspace = true
