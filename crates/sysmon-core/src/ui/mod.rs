//! Presentation primitives that are still pure: the palette, and the glyph
//! arithmetic that draws bars and sparklines. No renderer, so a contrast or a
//! width property can be asserted without a terminal.

pub mod theme;
